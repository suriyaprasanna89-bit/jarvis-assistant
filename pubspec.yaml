name: jarvis_assistant
description: "JARVIS HUD Assistant"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  webview_flutter: ^4.7.0
  flutter_pdfview: ^1.3.2
  file_picker: ^8.0.0
  camera: ^0.10.5+9
  flutter_tts: ^4.0.2

flutter:
  uses-material-design: true
  
